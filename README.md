# rgw-admin - RGW Admin access Python API

The library cloudscale is currently using to access the RGW admin API. It's not feature
complete, but might get there at some point.

## Development Setup

```shell
pre-commit install
make venv
```
